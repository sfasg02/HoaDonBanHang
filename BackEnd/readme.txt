Khi import, Python sẽ tính theo vị trí file main,
do đó nếu trong khai báo ở file trong /Src vẫn phải để vị trí tương đối theo vị trí của main.py

Để patch được thì attribute phải là optional => Phải verify data ngay từ khi khởi tạo.